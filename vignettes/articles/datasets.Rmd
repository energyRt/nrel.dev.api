---
title: "datasets"
---

```{r, include = FALSE}
knitr::opts_chunk$set(
  collapse = TRUE,
  comment = "#>"
)
```

```{r setup}
library(nrel.dev.api)
```

## Data availability by collections


```{r, fig.height=7, fig.width=10, fig.cap="Data availability by collections, geographical scope, and key parameters.", echo=FALSE, eval=TRUE, include=TRUE}
# install.packages("BiocManager")
# BiocManager::install("EBImage")
library("EBImage")
fld <- "../../man/figures/collections/"

# imgs <- paste0("../images/poa_pvwatts_merra/", sample(list.files(fld), 5))
imgs <- file.path(fld, list.files(fld))
# imgs <- paste0("images/", list.files(fld))
# EBImage::display(readImage(imgs), method = "browser", width = 1200, height = 820)
EBImage::display(readImage(imgs), method = "browser", width = 1000, height = 700)
# EBImage::display(readImage(imgs), method = "browser", width = 800, height = .7*800)
```
